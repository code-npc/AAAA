package ArrayList;

public class FoodTest {
    public static void main(String[] args) {
        //第一步，创建一个food类，生成菜品对象，保存菜品数据
        // 创建一个菜品操作类，用来封装需要的操作
        FoodOperator operator = new FoodOperator();
        operator.start();
    }

}
